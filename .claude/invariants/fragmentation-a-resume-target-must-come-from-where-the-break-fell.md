# A resume target must come from where the break fell

_CSS Fragmentation Level 3. Tracker: [#320](https://github.com/jhaygood86/PeachPDF/issues/320)._

, never from "the pass after this one". A box can be placed far down the document — past a tall spacer, or by an engine positioning its own children — so the fragmentainer it overflows is not in general the next one.
