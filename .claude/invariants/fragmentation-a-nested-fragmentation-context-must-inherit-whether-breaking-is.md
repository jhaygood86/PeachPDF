# A nested fragmentation context must inherit whether breaking is live at all

_CSS Fragmentation Level 3. Tracker: [#320](https://github.com/jhaygood86/PeachPDF/issues/320)._

One established inside a suppressed scope records a resumption record the enclosing engine never reads — **content silently dropped**. Measured at five of twelve items.
